<!doctype html>
<html lang="id">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Floating Sticker</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      background: #f8f8f8;
    }

    .sticker {
      position: absolute;
      right: 20px;
      bottom: 20px;
      width: 160px;      /* bisa ubah ukuran di sini */
      height: auto;
      user-select: none;
      -webkit-user-drag: none;
      animation: floatY 3.5s ease-in-out infinite;
    }

    @keyframes floatY {
      0%   { transform: translateY(0); }
      50%  { transform: translateY(-15px); }
      100% { transform: translateY(0); }
    }
  </style>
</head>
<body>
  <img class="sticker" src="https://i.imgur.com/L4kMwdL.png" alt="Floating Sticker">
</body>
</html>

